

# Make Offline Logo Bigger

Two size increases in `src/pages/LandingPage.tsx`:

| Location | Current | New |
|----------|---------|-----|
| Header (line 30) | `h-7` | `h-10` |
| Footer (line 306) | `h-6` | `h-9` |

Also check `src/pages/Index.tsx` loading screen logo (`h-12`) — that one is already reasonably sized, no change needed.

