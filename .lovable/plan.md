

# Replace Hero Image

Copy the uploaded yacht image to `src/assets/` replacing `hero-travel.jpg`, then update the import in `LandingPage.tsx`.

## Changes

| File | Change |
|------|--------|
| `src/assets/hero-travel.jpg` | Replace with the uploaded yacht/champagne image |
| `src/pages/LandingPage.tsx` | Update import path and alt text for the new hero image |

The uploaded image (women clinking champagne on a yacht at golden hour) will be copied to `src/assets/hero-travel.jpg`, replacing the current hero image. The alt text on the `<img>` tag will be updated to match the new image content.

